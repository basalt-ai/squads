---
name: example-playbook
description: "TODO: the squad-wide playbook. One or two sentences on what shared method it teaches and when an agent should load it — this text is how an agent decides to use the skill, so be specific and concrete."
---

# Example playbook

<!-- TODO: This is a squad-wide skill — it is referenced from the top-level
     `skills` array in manifest.json and copied into EVERY agent in the squad at
     install (squads do not share skills by reference — each agent gets its own
     copy). Write the shared method the squad's agents follow. A skill is a
     procedure, not reference docs: write it as steps. Strip every TODO before
     publishing. -->

## When to use this

TODO: the trigger — what situation makes an agent load this skill.

## The procedure

1. TODO: first step.
2. TODO: second step.
3. TODO: ...

## Notes

TODO: edge cases, what good output looks like, what to avoid.
